depends+=(firefox)

package() {
  local id="$(grep -Pom1 '(?<=<em:id>).*(?=</em:id>)' install.rdf)"
  local destdir="$pkgdir/usr/lib/firefox/browser/extensions/$id"
  install -d $destdir
  cp --no-preserve=ownership,mode -r * $destdir
}

# vim: filetype=sh
